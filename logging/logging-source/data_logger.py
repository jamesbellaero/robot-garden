

class DataLogger:
    def init(self):
        self.file_handles = []
        # Easily searchable lists
        self.directory_list = []
        self.file_list = []
    
    def log(self, measurement):
        # Create a new directory if necessary
        if(not directory_list.contains(measurement.source)):
            pass
        # Create a new file if necessary
        if(not file_list.contains(measurement.source + measurement.meas_type))
            pass
        # Write to file
        

