# This file does three things. 
# First, it takes in a file that relates input sources to their configuration files
# Second, it reads those config files and creates a key-value map
# Third, it returns a list of these maps paired with their associated input for use by the data_parser 